scripts:
  echo:
    name: "Display name"
    run: echo "$ENV_VAR_TEST_2"
    env_var:
      ENV_VAR_TEST_1: "Hello world 1"
      ENV_VAR_TEST_2: "Hello world 2"
    # run: ""
  echo_two_line:
    run: |
      echo "First Line"
      echo "Second line"

  ping:
    run: |
      ping -c 2 8.8.8.8
      ping -c 2 google.com
