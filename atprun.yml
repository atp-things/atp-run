scripts:
  echo:
    name: "Display name"
    run: echo "Hello world"
    # run: ""
  echo_two_line:
    run: |
      echo "First Line"
      echo "Second line"

  ping:
    run: |
      ping -c 2 8.8.8.8
      ping -c 2 google.com
